# Facebook Watchman Docker file

This project packs into a Docker prepared image the Facebook Watchman software.

- It will try to detect mofified data from /data directory
- The image is alpine edge based
